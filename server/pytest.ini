[pytest]
# Run from the server/ directory so `agentlamp_server` (the package dir) and the
# `tests` package are both importable. conftest.py also adds server/ to sys.path.
pythonpath = .
testpaths = tests
addopts = -ra
