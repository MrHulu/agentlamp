# Manual Adapter

Manual adapter is the first end-to-end source.

It should support:

- manual session upsert
- manual session status
- manual quota window
- manual alert raise/clear

Use it to prove cloud ingest, sanitizer, frame generation, and simulator before Codex/Claude hooks.

