{
  "name": "agentlamp-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "AgentLamp Cloudflare relay (Worker + Durable Object + KV) — the VALIDATE-ONLY cloud gate (build-spec I1).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.16.12",
    "@cloudflare/workers-types": "4.20260602.1",
    "@types/node": "24.12.4",
    "typescript": "5.9.3",
    "vitest": "4.1.8",
    "wrangler": "4.97.0"
  }
}
